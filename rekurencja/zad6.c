#include <stdio.h> /* /c/podstawowe typy danych operacje na bitach */
/*int podzbior(int n, int m) {
  char c[n];
  if (n==0) {
    printf("{} ");
  }
  else {
    sprintf(c, "%d", m);
    podzbior(n-1, m-power(10,n-1));
    printf("{");
    for(int i=1;i*i<=n;i++) {
      if (i!=1) {
        printf(",");
      }
      printf("%d", i);
    }
    printf("} ");
  }
  return 0;
}
int main() {
  int n,m=0;
  scanf("%d", &n);
  if (n<0) {
    printf("wrong input\n");
  }
  else {
    for(int i=0;i<n;i++) {
      m=m+1*power(10,i);
    }
    podzbior(n,m);
  }
}
*/
